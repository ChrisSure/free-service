{% extends 'layouts/base.html.twig' %}

{% block title %}Admins{% endblock %}

{% block breadcrumbs %}
    <ol class="breadcrumb">
        <li class="breadcrumb-item"><a href="{{ path('home') }}">Home</a></li>
        <li class="breadcrumb-item active">Admins</li>
    </ol>
{% endblock %}

{% block body %}
    <a href="#" class="btn btn-success mb-4">Create new</a>

    <div class="card mb-4">
        <div class="card-body">
            {{ form_start(form) }}
            <div class="row">
                <div class="col-md-2">{{ form_widget(form.email) }}</div>
                <div class="col-md-2">{{ form_widget(form.status) }}</div>
                <div class="col-md-2">{{ form_widget(form.role) }}</div>
                <div class="col-md-2">
                    <button type="submit" class="btn btn-primary">Filter</button>
                    <a href="{{ path('admins') }}" class="btn btn-outline-secondary">Reset</a>
                </div>
            </div>
            {{ form_end(form) }}
        </div>
    </div>

    {% if admins is empty %}
        Empty
    {% else %}
    <table class="table">
        <thead>
        <tr>
            <th scope="col">Id</th>
            <th scope="col">Email</th>
            <th scope="col">Role</th>
            <th scope="col">Status</th>
            <th scope="col">{{ knp_pagination_sortable(admins, 'Created', 'created_at') }}</th>
        </tr>
        </thead>
        <tbody>
        {% for admin in admins %}
        <tr>
            <th scope="row">{{ admin.id }}</th>
            <td><a href="{{ path('admins_detail', {id: admin.id}) }}" class="show_link">{{ admin.email }}</a></td>
            <td>
                {% if (admin.roles[0] == 'ROLE_SUPER_ADMIN') %}
                    <span class="badge badge-secondary">Super admin</span>
                {% elseif(admin.roles[0] == 'ROLE_ADMIN') %}
                    <span class="badge badge-secondary">Admin</span>
                {% else %}
                    <span class="badge badge-secondary">Moderator</span>
                {% endif %}
            </td>
            <td>
                {% if (admin.status == 'active') %}
                    <span class="badge badge-success">Active</span>
                {% elseif(admin.status == 'blocked') %}
                    <span class="badge badge-danger">Blocked</span>
                {% else %}
                    <span class="badge badge-primary">New</span>
                {% endif %}
            </td>
            <td>{{ admin.created_at|date('Y-m-d') }}</td>
        </tr>
        {% endfor %}
        </tbody>
    </table>

    <div class="navigation">
        {{ knp_pagination_render(admins) }}
    </div>
    {% endif %}

{% endblock %}