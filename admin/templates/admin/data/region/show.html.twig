{% extends 'layouts/base.html.twig' %}

{% block title %}Region detail{% endblock %}

{% block breadcrumbs %}
    <ol class="breadcrumb">
        <li class="breadcrumb-item"><a href="{{ path('home') }}">Home</a></li>
        <li class="breadcrumb-item"><a href="{{ path('data_region_index') }}">Regions</a></li>
        <li class="breadcrumb-item active">Region detail</li>
    </ol>
{% endblock %}

{% block body %}
    <h1>Region detail</h1>
    <a class="btn btn-primary" href="{{ path('data_region_edit', {'id': region.id}) }}">edit</a>

    <table class="table mt-3">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ region.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ region.name }}</td>
            </tr>
        </tbody>
    </table>

    {{ include('admin/data/region/_delete_form.html.twig') }}
{% endblock %}